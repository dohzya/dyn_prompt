low:
  PROMPT:  "$df(%B*%b){ }$br(){ }$vm()%# "
  RPROMPT: ""
medium:
  PROMPT:  "%1~ $br()$fl() $vm()%# "
  RPROMPT: "[$rvm()] %m %*"
high:
  PROMPT:  "%1~ $br()$fl() $vm()%# "
  RPROMPT: " %B%~%b) [$rvm()] %m %*"
