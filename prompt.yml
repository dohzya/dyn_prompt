low:
  PROMPT:  "$df(%B*%b){ }$nm()$rb(){ }$vm()%# "
  RPROMPT: ""
medium:
  PROMPT:  "%1~${ }nm()$fl()$rb() $vm()%# "
  RPROMPT: "[$rvm()] %m %*"
high:
  PROMPT:  "%1~${ }nm()$fl()$rb() $vm()%# "
  RPROMPT: " %B%~%b) [$rvm()] %m %*"
